{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "docker run -ti --rm --name animals -v `pwd`:/srv/jekyll -p 4000:4000 jekyll/jekyll jekyll serve"
        }
    ]
}